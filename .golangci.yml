#
# (c) Copyright IBM Corp. 2025
#

version: "2"
formatters:
  enable:
    - gofmt
    - goimports
    - golines
linters:
  default: none
  enable:
    - govet
    - ineffassign
    - unused
    - misspell
    - exhaustive
    - errcheck
